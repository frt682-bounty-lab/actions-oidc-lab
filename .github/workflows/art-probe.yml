name: art-probe
on: [push, workflow_dispatch]
jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - run: echo SECRET-ARTIFACT-DATA > secret.txt
      - uses: actions/upload-artifact@v4
        with:
          name: secret-art
          path: secret.txt
